node_modules
.next
.env
.env.local
*.log

# Local data snapshots (sheet rollback backups)
data-snapshots/

# Local double-click launcher (machine-specific)
*.command
.revie/
.vercel
.env*

# TypeScript incremental build cache — machine-specific, and it conflicted on every rebase.
tsconfig.tsbuildinfo
